# Windows 방화벽 설정 가이드

## 🔓 포트 열기 (Windows 10/11)

### 방법 1: GUI 사용

1. **Windows 방화벽 열기**
   - `Windows 키` + `R`
   - `wf.msc` 입력 후 Enter

2. **인바운드 규칙 추가**
   - 왼쪽 메뉴: "인바운드 규칙" 클릭
   - 오른쪽: "새 규칙..." 클릭

3. **규칙 유형 선택**
   - "포트" 선택 → 다음

4. **프로토콜 및 포트**
   - TCP 선택
   - 특정 로컬 포트: `8080` 입력 → 다음

5. **작업**
   - "연결 허용" 선택 → 다음

6. **프로필**
   - 도메인, 개인, 공용 모두 체크 → 다음

7. **이름**
   - 이름: "평가 시스템 웹 서버"
   - 설명: "Working Together Review 시스템" → 마침

### 방법 2: 명령어 사용 (관리자 권한)

```cmd
# PowerShell을 관리자 권한으로 실행 후:

netsh advfirewall firewall add rule name="평가시스템" dir=in action=allow protocol=TCP localport=8080

# 규칙 삭제 (필요 시)
netsh advfirewall firewall delete rule name="평가시스템"
```

## 🖥️ IP 주소 확인

```cmd
# 명령 프롬프트에서:
ipconfig

# IPv4 주소를 확인하세요
# 예: 192.168.1.100
```

## 🧪 연결 테스트

### 같은 컴퓨터에서 테스트
1. 브라우저 열기
2. `http://localhost:8080` 접속
3. 로그인 페이지가 보이면 성공!

### 다른 컴퓨터에서 테스트
1. 다른 PC의 명령 프롬프트에서:
```cmd
ping 192.168.1.100
```
2. 응답이 오면 브라우저로 접속:
   `http://192.168.1.100:8080`

## ⚠️ 주의사항

- 회사 보안 정책에 따라 IT 부서 승인이 필요할 수 있습니다
- VPN 사용 시 추가 설정이 필요할 수 있습니다
- 백신 프로그램이 포트를 차단할 수 있습니다

## 🔍 문제 해결

### "연결할 수 없음" 에러
1. 서버가 실행 중인지 확인
2. 방화벽 규칙 재확인
3. 백신 프로그램 예외 추가
4. Windows Defender 방화벽 상태 확인

### 다른 PC에서만 안 될 때
1. 같은 네트워크인지 확인 (Wi-Fi vs 유선)
2. 공유기 설정 확인
3. IT 부서에 네트워크 정책 문의
